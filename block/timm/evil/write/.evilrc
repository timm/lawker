### user config stuff
Work   = $(HOME)/opt/evil
Here   = $(HOME)/svns/lawker/block/timm# 
Evil   = $(Here)/evil# ... root

Writes = $(Evil)/write# .. directory of unique function names
Base   = $(Write)/evil# .. start up utils
Read   = $(Evil)/read# ... html docs, in the repository

Test   = $(Work)/test# ... directory, flattenned
Run    = $(Work)/run# .... delivered code.
