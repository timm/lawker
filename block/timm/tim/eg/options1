. ~/.timrc

./tim -e -C s2a.awk -C saya.awk -C options.awk

$Pawk -f s2a.awk -f saya.awk -f options.awk --source ' BEGIN { 

	s2a("_a,1,b,,_c,3",Opt)
	arg2options(Opt)
	saya(Opt,"opt")

}' -a 2 -b -c 20
