#
# See if a start and end position fall within any entries of an array of start and
# end positions.
#
# Array structure: the index is the start position, the value is the end position
#
# needs the asize function

function searchLine(segments,start,end,      segCount) {
	segCount = asize(segments)
}
